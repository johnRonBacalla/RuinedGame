package ui;

import input.KeyInput;
import input.MouseInput;

public class PlayUI extends UI{
    public PlayUI(KeyInput keyInput, MouseInput mouseInput) {
        super(keyInput, mouseInput);
    }
}
