package entity.placeable.towers;

import entity.GameObject;
import gfx.SpriteLibrary;

public class Tower extends GameObject {
    public Tower(double x, double y, SpriteLibrary sprites) {
        super(x, y, sprites);
    }
}
